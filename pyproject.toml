[project]
name = "circleup-comfort-marker"
version = "0.1.0"
description = "Mark prospectus numbers requiring accountant comfort on PDFs."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pymupdf>=1.24.0",
]

[project.optional-dependencies]
dev = []

[project.scripts]
comfort-marker = "comfort_marker.cli:main"
